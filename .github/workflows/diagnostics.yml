name: gitbox diagnostics

on:
  push:
  pull_request:

jobs:
  diagnostics:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4

      - name: health scan
        shell: pwsh
        run: ./gitbox.ps1 H -Plain
